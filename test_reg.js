const { app } = require('electron'); app.whenReady().then(async () => { try { const auth = require('./backend/handlers/auth'); const result = await auth.registerUser(null, { nome: 'Aprile', cognome: 'Nunzio', email: 'aprilenunzio88@gmail.com', pin: '111111', password: 'password123', networkName: 'Rodolico' }); console.log('RESULT:', result); } catch(e) { console.error('ERROR:', e); } app.quit(); });
